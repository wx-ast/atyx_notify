name: 'atyx_notify'
description: 'Отправка уведомлений через atyx.ru notify API'

inputs:
  message:
    description: 'Сообщение для отправки'
    required: true

runs:
  using: 'docker'
  image: 'Dockerfile'
