name: 'atyx_notify'
description: 'Отправка уведомлений через atyx.ru notify API'

runs:
  using: 'docker'
  image: 'Dockerfile'
  env:
    API_KEY: ${{ inputs.api_key }}
    API_SECRET: ${{ inputs.api_secret }}
    MESSAGE: ${{ inputs.message }}
